<%= form_for(@farm) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= link_to "Previous", "#", class: "prev btn btn-small" %>
  <%= link_to "Next", "#",     class: "next btn btn-small" %>

  <%= f.submit "Next", class: "hidden btn btn-small " %>
<% end %>

